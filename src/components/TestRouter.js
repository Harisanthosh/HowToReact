import React from "react";
//import Browser router and add the <Router> tags around <App /></Router>
// Instead of <a href ></a> replace with <Link to=> from "react-router-dom"
// Use <Route path="/" component={}/>, use exact to match the one
// Use Switch to ensure only one of the matches is displayed while skipping the other 
// Use a catch all to match the invalid path and use render to pass react component JSX here